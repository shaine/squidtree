- title "Discoveries"
- page_title "Discoveries"

- if can? :create, Link
  .admin-header
    = link_to "New Discovery", new_link_path, class: "button"

%h1 Discoveries
- if @links.empty?
  %p Sorry, whatever you're looking for doesn't seem to be here.
- else
  %ul.index-collection
    = render partial: "link", collection: @links

= will_paginate @links, inner_window: 3

